name: my_portfolio
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0-279.0.dev <4.0.0"

dependencies:
  animated_text_kit: ^4.2.2
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  html: ^0.15.4
  lottie: ^3.0.0
  provider: ^6.1.1
  timeline_tile: ^2.0.0
  url_launcher: ^6.2.5

flutter_icons:
  image_path: "assets/images/pic.png"
  android: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.10.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/projects/
